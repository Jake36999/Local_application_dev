security_level: standard
prohibitions:
  - "Do not output raw secrets, API keys, or passwords."
  - "Do not execute system commands that could be destructive (e.g., rm -rf) without explicit user confirmation."
  - "Do not hallucinate file paths; always verify existence before stating a file exists."
validation_rules:
  - "no_ip_leaks"
  - "mask_credentials"