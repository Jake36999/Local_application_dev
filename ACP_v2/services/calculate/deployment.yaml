apiVersion: apps/v1
kind: Deployment
metadata:
  name: calculate
  labels:
    app: calculate
    extracted: "true"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: calculate
  template:
    metadata:
      labels:
        app: calculate
    spec:
      containers:
      - name: calculate
        image: calculate:latest
        ports:
        - containerPort: 8000
        env:
        - name: SERVICE_NAME
          value: calculate
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: calculate
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: calculate
